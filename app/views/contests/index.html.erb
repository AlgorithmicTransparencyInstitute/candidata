<div class="min-h-screen bg-gray-50">
  <%= render "shared/public_navbar" %>

  <main class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900">Election Contests</h1>
      <p class="mt-2 text-gray-600">Browse individual races and contests</p>
    </div>

    <!-- Filters -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
      <%= form_with url: contests_path, method: :get, class: "space-y-4" do |f| %>
        <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Year</label>
            <%= select_tag :year, options_for_select([['All Years', '']] + @years.map { |y| [y, y] }, params[:year]),
                class: "w-full px-3 py-2 border border-gray-300 rounded-md" %>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">State</label>
            <%= select_tag :state, options_for_select([['All States', '']] + @states.map { |s| [s, s] }, params[:state]),
                class: "w-full px-3 py-2 border border-gray-300 rounded-md" %>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Party</label>
            <%= select_tag :party, options_for_select([['All Parties', '']] + @parties.map { |p| [p, p] }, params[:party]),
                class: "w-full px-3 py-2 border border-gray-300 rounded-md" %>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Type</label>
            <%= select_tag :contest_type, options_for_select([['All Types', '']] + @contest_types.map { |t| [t.titleize, t] }, params[:contest_type]),
                class: "w-full px-3 py-2 border border-gray-300 rounded-md" %>
          </div>
          <div class="flex items-end">
            <%= button_tag type: 'submit', class: "w-full px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700" do %>
              Filter
            <% end %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Results Count -->
    <div class="text-sm text-gray-600 mb-4">
      Showing <%= @contests.total_count %> <%= 'contest'.pluralize(@contests.total_count) %>
    </div>

    <!-- Contests List -->
    <div class="space-y-4">
      <% @contests.each do |contest| %>
        <%= link_to contest_path(contest), class: "block bg-white rounded-lg shadow-sm border border-gray-200 p-6 hover:shadow-md hover:border-indigo-300 transition-all" do %>
          <div class="flex items-start justify-between">
            <div class="flex-1">
              <div class="flex items-center gap-2 mb-2">
                <span class="inline-block px-2 py-1 text-xs font-semibold rounded <%= contest.party == 'Democratic' ? 'bg-blue-100 text-blue-800' : contest.party == 'Republican' ? 'bg-red-100 text-red-800' : 'bg-gray-100 text-gray-800' %>">
                  <%= contest.party || 'Nonpartisan' %>
                </span>
                <span class="text-sm text-gray-600"><%= contest.contest_type.titleize %></span>
                <span class="text-sm text-gray-500"><%= contest.ballot&.state %></span>
              </div>

              <h3 class="text-lg font-semibold text-gray-900 mb-1">
                <%= contest.office&.title || 'Office Not Specified' %>
                <% if contest.office&.seat.present? %>
                  <span class="text-gray-600">(<%= contest.office.seat %>)</span>
                <% end %>
              </h3>

              <p class="text-sm text-gray-600">
                <%= contest.date.strftime('%B %d, %Y') if contest.date %>
                · <%= pluralize(contest.candidates.count, 'candidate') %>
              </p>
            </div>

            <svg class="w-5 h-5 text-indigo-600 flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
          </div>
        <% end %>
      <% end %>
    </div>

    <% if @contests.empty? %>
      <div class="text-center py-12 bg-white rounded-lg shadow-sm border border-gray-200">
        <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
        </svg>
        <p class="mt-2 text-gray-500">No contests found matching your filters.</p>
      </div>
    <% end %>

    <!-- Pagination -->
    <% if @contests.total_pages > 1 %>
      <div class="mt-6">
        <%= paginate @contests %>
      </div>
    <% end %>
  </main>
</div>
