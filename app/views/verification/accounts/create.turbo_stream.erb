<%= turbo_stream.prepend "#{@account.channel_type.parameterize}-accounts-list" do %>
  <%= render "verification/accounts/account_row", account: @account, assignment: @assignment %>
<% end %>

<%= turbo_stream.replace "add-account-form-#{@account.channel_type.parameterize}" do %>
  <%= render "verification/accounts/add_account_form", person: @person, channel_type: @account.channel_type %>
<% end %>
