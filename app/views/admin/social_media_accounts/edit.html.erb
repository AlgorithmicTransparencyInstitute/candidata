<div class="max-w-3xl mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to Account", admin_social_media_account_path(@account), class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <div class="bg-white rounded-lg shadow">
    <div class="px-6 py-4 border-b border-gray-200">
      <h1 class="text-2xl font-bold text-gray-900">Edit Social Media Account</h1>
      <p class="mt-1 text-sm text-gray-600"><%= @account.person.full_name %> - <%= @account.platform %></p>
    </div>
    <div class="px-6 py-4">
      <%= render "form", account: @account %>
    </div>
  </div>
</div>
