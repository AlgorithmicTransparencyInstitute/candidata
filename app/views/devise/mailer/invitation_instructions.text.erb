You're invited to join CandiData!

Hello<%= @resource.name.present? ? " #{@resource.name}" : "" %>!

You've been invited to join CandiData as a <%= @resource.role.titleize %>. CandiData is a platform for tracking and validating candidate data for elections across the United States.

As a researcher, you'll help update and verify social media accounts and other data for political candidates — work that directly supports algorithmic transparency and accountability.

Accept your invitation and get started:
<%= accept_invitation_url(@resource, invitation_token: @token) %>

You can set up your account with a password, or sign in with your Google account — whichever you prefer.

This invitation will expire in <%= distance_of_time_in_words(@resource.class.invite_for) %>. If you believe this was sent in error, you can safely ignore this email.

— CandiData / Algorithmic Transparency Institute
